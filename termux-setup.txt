
#Expose ChromeOS storage:

termux-setup-storage

# Setup SSH:
apt install openssh 
ssh-keygen
cp * /storage/downloads/

#usdful commands:
#whoami - username
#ifconfig - IP address
#u0_a54@100.115.92.2
#pkill sshd

sshd

#Install General Apps:

apt install nano

# Install Data & Analytics packages (including jupyter)

pkg update
apt-get update
apt install python python-dev clang fftw
LDFLAGS=" -lm -lcompiler_rt" pip install numpy
apt install freetype freetype-dev libpng libpng-dev pkg-config
LDFLAGS=" -lm -lcompiler_rt" pip install matplotlib
apt install libzmq libzmq-dev
LDFLAGS=" -lm -lcompiler_rt" pip install jupyter
LDFLAGS=" -lm -lcompiler_rt" pip install pandas

#=========================================================

# BACKUP TERMUX
cd /data/data/com.termux/files
tar -cvzf ~/storage/downloads/termux/termux_20180217.tgz home usr

# RESTORE TERMUX
cd /data/data/com.termux/files
tar -xvzf ~/storage/downloads/termux/termux_20180217.tgz
